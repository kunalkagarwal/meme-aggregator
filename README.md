# Meme-Aggregator

This project implements a real-time meme coin aggregation service (REST + WebSocket).

Run locally:

1. npm install
2. npm run dev

Environment variables:
- REDIS_URL (optional)
- POLL_INTERVAL_SECONDS (optional, default 10)

See the canvas in the ChatGPT response for full architecture and code description.
